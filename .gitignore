# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Node (previewer, if a build step is added later)
node_modules/

# Solver outputs (layouts, previews, schematics) - not source
out/
*.litematic
*.schematic

# Structure dump is local-only (see docs/dataset-extraction/plan.md): the extractor
# regenerates ~190 multiblock docs on demand; only the two curated fixtures are committed.
data/multiblocks/*.json
!data/multiblocks/_meta.json
!data/multiblocks/gregtech_machine_1000.json
!data/multiblocks/gregtech_machine_1001.json

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
